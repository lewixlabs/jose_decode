{
  "name": "jose-decode-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "serve:dist": "vite preview --host 127.0.0.1 --port 4173",
    "build:standalone": "node scripts/build-standalone.mjs"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
